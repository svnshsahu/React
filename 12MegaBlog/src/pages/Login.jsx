// import React from 'react'
import { Login as LoginComponent } from '../components'
// The error is because React components must start with an uppercase letter. 
// The error is because React components must start with an uppercase letter.
function Login() {
    return (
        <div className="py-8">
            <LoginComponent/>
        </div>
    )
}

export default Login
